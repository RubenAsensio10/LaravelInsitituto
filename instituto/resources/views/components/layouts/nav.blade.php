<nav class="bg-nav h-nav flex flex-row justify-start items-center space-x-2">
    <span class="btn btn-active btn-primary ml-2 rounded-lg cursor-default">{{ __('messages.about') }}</span>
    <span class="btn btn-active btn-warning rounded-lg cursor-default">{{ __('messages.contact') }}</span>
    <span class="btn btn-active btn-primary rounded-lg cursor-default">{{ __('messages.news') }}</span>
    <span class="btn btn-active btn-warning rounded-lg cursor-default">{{ __('messages.references') }}</span>
</nav>